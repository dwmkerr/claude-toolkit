skill: protocols:exploration-protocol
model: haiku

tests:
  - id: explore-feature
    prompt: "I want to explore adding OAuth2 authentication before we commit to building it"
    should_trigger: true

  - id: start-exploration
    prompt: "start an exploration protocol for the new billing feature"
    should_trigger: true

  - id: create-task-folder
    prompt: "create a task folder to explore migrating to PostgreSQL"
    should_trigger: true

  - id: unrelated-fix
    prompt: "fix the failing unit test in auth.test.ts"
    should_trigger: false

  - id: unrelated-build
    prompt: "build and deploy the application to staging"
    should_trigger: false
