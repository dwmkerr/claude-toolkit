import { defineAppSetup } from "@slidev/types";

export default defineAppSetup(({ app }) => {
  // Georgia (headings) and Arial (body) are system fonts — no loading needed.
  // Bower and McKinsey Sans are the original QB fonts but are not publicly
  // licensed. Georgia and Arial are the standard PowerPoint substitutes.
});
