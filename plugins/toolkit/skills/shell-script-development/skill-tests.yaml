skill: toolkit:shell-script-development
model: haiku

tests:
  - id: create-bash-script
    prompt: "create a bash script that backs up a PostgreSQL database to a local directory"
    should_trigger: true

  - id: write-shell-script
    prompt: "write a shell script that monitors disk usage"
    should_trigger: true

  - id: unrelated-react
    prompt: "add a new React component for user profiles"
    should_trigger: false
